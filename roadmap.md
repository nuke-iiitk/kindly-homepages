# Roadmap

- [x] Diagnose preview build failures (missing `dev` / `build:dev` scripts after project swapped to Expo).
- [x] Add `dev` + `build:dev` scripts so the Lovable preview serves the Expo web app on port 8080.
- [x] Verify dev server healthy: HTTP 200, JS bundle compiles (5 MB), full app renders in browser.
