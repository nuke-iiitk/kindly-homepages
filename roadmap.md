# Roadmap

- [x] Diagnose preview build failures (missing `dev` / `build:dev` scripts after project swapped to Expo).
- [ ] Add `dev` + `build:dev` scripts so the Lovable preview serves the Expo web app on port 8080.
- [ ] Verify dev server becomes healthy and the app loads in the preview.
